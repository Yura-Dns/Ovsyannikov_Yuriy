package ru.job4j.calculate;
/**
 * M_V (Maximum_value - Определение максимального значения)
 */
public class M_V {
    private int result;
    public int max(int first, int second) {
        return this.result = (first>second ? first:second);
    }
}